{{- $c := index (.Values.podDisruptionBudget | default dict) "controller" | default dict }}
{{- if $c.enabled }}
{{- include "skyflo.podDisruptionBudget" (dict "root" $ "config" $c "fullname" (include "skyflo.controller.fullname" .)) }}
{{- end }}
