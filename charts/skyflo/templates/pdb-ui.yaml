{{- $c := index (.Values.podDisruptionBudget | default dict) "ui" | default dict }}
{{- if $c.enabled }}
{{- include "skyflo.podDisruptionBudget" (dict "root" $ "config" $c "fullname" (include "skyflo.ui.fullname" .)) }}
{{- end }}
