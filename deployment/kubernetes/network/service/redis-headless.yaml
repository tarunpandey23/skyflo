apiVersion: v1
kind: Service
metadata:
  name: skyflo-ai-redis-headless
  namespace: skyflo-ai
spec:
  clusterIP: None
  ports:
  - port: 6379
    targetPort: redis
    protocol: TCP
    name: redis
  selector:
    app: skyflo-ai-redis
